#!/bin/bash

sudo iptables -A PREROUTING -t nat -i eth0 -p tcp --dport 443 -j REDIRECT --to-port 25565

ufw allow 22/tcp
ufw allow 25565/tcp
ufw allow 64738/tcp
ufw allow 64738/udp
ufw --force enable
